package com.example.jetpack_compose_foot_calendar.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Material 3 colour palette for the application theme.
 *
 * The `80` suffix variants are used for the **dark** colour scheme and the `40` variants
 * for the **light** colour scheme, following the Material 3 tonal palette convention.
 */

/** Primary colour for the dark theme. */
val Purple80 = Color(0xFFD0BCFF)

/** Secondary colour for the dark theme. */
val PurpleGrey80 = Color(0xFFCCC2DC)

/** Tertiary colour for the dark theme. */
val Pink80 = Color(0xFFEFB8C8)

/** Primary colour for the light theme. */
val Purple40 = Color(0xFF6650a4)

/** Secondary colour for the light theme. */
val PurpleGrey40 = Color(0xFF625b71)

/** Tertiary colour for the light theme. */
val Pink40 = Color(0xFF7D5260)